# Example .watchwireignore (gitignore-style). Copy to repo root or run `watchwire init`.
# Local-only; never sent off-box.

.git/
node_modules/
.venv/
venv/
__pycache__/
dist/
build/
*.min.js
*.lock
package-lock.json
yarn.lock
pnpm-lock.yaml
poetry.lock
Cargo.lock
go.sum
